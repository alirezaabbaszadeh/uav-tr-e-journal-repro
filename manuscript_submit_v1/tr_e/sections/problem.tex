The problem setting follows a multi-UAV pickup-and-delivery scenario with soft TW penalties and communication-risk terms. Policy gates are enforced by instance size regime to prevent invalid inference on scalability slices.
