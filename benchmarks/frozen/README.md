# Frozen Benchmarks

This folder stores deterministic benchmark instances generated by:

```bash
python -m uavtre.run_benchmarks --config configs/base.json --profile main_table --benchmark-dir benchmarks/frozen
```

Each JSON file corresponds to one scenario specification and can be replayed for consistent experiments.
